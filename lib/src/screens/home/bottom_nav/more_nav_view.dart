import 'package:flutter/material.dart';

class MoreNavView extends StatelessWidget {
  const MoreNavView({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("More");
  }
}
